<%= form_for(@containment) do |f| %>
  <% if @containment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@containment.errors.count, "error") %> prohibited this containment from being saved:</h2>

      <ul>
      <% @containment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cont_start_date %><br />
    <%= f.date_select :cont_start_date %>
  </div>
  <div class="field">
    <%= f.label :revision_date %><br />
    <%= f.date_select :revision_date %>
  </div>
  <div class="field">
    <%= f.label :cont_end_date %><br />
    <%= f.date_select :cont_end_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
