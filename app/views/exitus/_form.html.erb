<%= form_for(@exitu) do |f| %>
  <% if @exitu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exitu.errors.count, "error") %> prohibited this exitu from being saved:</h2>

      <ul>
      <% @exitu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :exitus_diagnosis %><br />
    <%= f.text_area :exitus_diagnosis %>
  </div>
  <div class="field">
    <%= f.label :place_exitus %><br />
    <%= f.text_field :place_exitus %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
